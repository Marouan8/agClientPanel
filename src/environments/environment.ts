// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase : {
    apiKey: "AIzaSyBQAD2_VFRLJEFBHU_gYDMLq51y4pVLx5o",
    authDomain: "clientpanelapp-e215d.firebaseapp.com",
    databaseURL: "https://clientpanelapp-e215d.firebaseio.com",
    projectId: "clientpanelapp-e215d",
    storageBucket: "clientpanelapp-e215d.appspot.com",
    messagingSenderId: "206972388048",
    appId: "1:206972388048:web:ff757569a2e8eccba554d5"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
